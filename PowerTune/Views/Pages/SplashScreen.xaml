<UserControl x:Class="PowerTune.Views.Pages.SplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:PowerTune.Views.Pages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource MainWindowBackground}">
    <Grid>
        <StackPanel Orientation="Vertical"
                    VerticalAlignment="Center">
            <Image VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Source="ms-appx:///Assets/FullLogo.png"
                   Width="440"
                   />
            <TextBlock x:Uid="SplasScreen_Title"
                       Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Uid="SplasScreen_Description"
                       Style="{StaticResource SubtitleTextBlockStyle}" />
        </StackPanel>
        <ProgressBar IsIndeterminate="True"
                     VerticalAlignment="Bottom"
                     Margin="0 0 0 1" 
                     HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
